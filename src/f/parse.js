
export default (
    (
        source,
        global,

        index,
        length
    ) => {
        var
            code = 0
        ;
        
        while (index < length) {
            index++;
        };

        return code;
    }
);
